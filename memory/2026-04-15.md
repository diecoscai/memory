# 2026-04-15

## Session summary
Memory repo sprint: README SEO rewrite, unified sync consolidation (Modes 1-3 → single pipeline), hooks explanation, Obsidian graph GIF added, OSS offer stack Linear tickets created, skill audit revealing 15-day journal gap.

## Key decisions
- Unified sync: single `/memory sync` scrapes 8 sources (session, journals, compaction, CC projects, CC plans, OpenClaw journals, OpenClaw topics) into one pipeline. Feeds HOT + WARM + COLD + wiki automatically.
- Dream now rebuilds wiki and calls `/memory sync` at end — two operations cover the full lifecycle.
- README repositioned around "AI amnesia" pain, "context engineering" term, "second brain" niche.

## Key learnings
- `session-stop-vault.sh` silently exits if SESSION-STATE.md doesn't exist → root cause of 15-day journal gap
- Installed skill at `~/.claude/skills/memory` is stale (old Mode 1-4 SKILL.md) — needs resync from repo
- GitHub doesn't render `<video>` tags for repo files — GIF is the reliable README format
- `avconvert` (macOS built-in) compresses MOV to MP4: 125MB → 11MB with Preset1280x720
- ffmpeg GIF palette optimization: `fps=8,scale=960,palettegen=max_colors=64` → 9.7MB for 10s clip

## OSS context engineering SEO research (2026-04-15)
Top queries: "Claude Code memory", "LLM persistent memory", "second brain Claude Code Obsidian", "context engineering", "AI agent amnesia fix"
Competing repos: coleam00/claude-memory-compiler, rohitg00/agentmemory, lucasrosati/claude-code-memory-setup
Differentiator angle that's working: no vector DB + local + cross-platform + LLM wiki built-in
