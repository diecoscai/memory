# Contributing

1. Fork the repo
2. Create a feature branch
3. Make changes — ensure `SKILL.md` version matches all manifests
4. Test hooks locally
5. Open a PR against `main`

All hook scripts must be executable and have `#!/bin/bash` shebang.
